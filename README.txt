Shuffle a pandas dataframe without too much code

The idea is to create an extra column - a 'shuffled index', if you will.
Sort the pandas dataframe according to the new column.
Set the new column to be the index.

C'est fini/ The End
